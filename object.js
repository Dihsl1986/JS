/* 

* Object -> Um tipo de dado que é chamado de estrutural
      * Object = objeto
      * Propriedade / Atributos
      * Funcionalidades /Métodos
      
      "Todo objeto tem atributos e métodos"

      { propriedade: "valor" }

*/

/*console.log({
      name: "Diego", // Criei um objeto que tem propriedade de name e valor Diego
      idade: 35,
      andar: function() { //Funcionalidade andar
            console.log('andar') // se é uma funcionalidade é um método pois esta dentro de um objeto.
      }
});*/

// Revisão de Objects

const person = { // <- Aqui estamos criando um objeto;
      name: "Diego",
      age: 35,
      weight: 76.6,
      isAdmin: true
}

// dentro do console.log eu coloco o nome do objeto seguido de ponto= . e o nome da propriedade desedaja

console.log(`${person.name} tem ${person.age} e pesa ${person.weight}`);
      //Nunca esqece o nome do objeto o ponte e a seguir a propriedade. 



 

