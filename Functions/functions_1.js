// Argumentos e parametros
// podemos declarar funções dentro de variaveis
            //<- é chamado de função anonima por que natem um nome no ()
            // function expression
            // function anonymous

const sum = function(number1, number2){//parâmetros da função(ou parameters)
             console.log(number1 + number2)
}

sum(443,21343); //(o number1 vai valer 2 e o number2 vai valer 3 
            // dentro do escopo acima);